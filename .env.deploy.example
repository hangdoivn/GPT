# ─────────────────────────────────────────────────────────────
#  Cấu hình deploy VPS bằng Docker Compose.
#  Trên VPS: copy file này thành `.env` rồi điền giá trị, sau đó chạy:
#     docker compose up -d --build
# ─────────────────────────────────────────────────────────────

# Domain đã trỏ A record về IP của VPS (KHÔNG kèm https://)
DOMAIN="app.hangdoistudio.vn"

# Mật khẩu cho database Postgres (tự đặt chuỗi bất kỳ, mạnh)
DB_PASSWORD="doi-mat-khau-manh-o-day"

# ── Facebook App ──
FACEBOOK_APP_ID="1424421672856268"
FACEBOOK_APP_SECRET="60256b8c95816c6646eaf9859e519fb5"
FACEBOOK_GRAPH_VERSION="v21.0"

# Chuỗi bí mật webhook & cron (đã tạo sẵn ngẫu nhiên, có thể đổi)
FACEBOOK_WEBHOOK_VERIFY_TOKEN="ec4ec95e8160d66bf4542ca985e6506e"
CRON_SECRET="aaba6a04e5356043770e2978526423b35e5a2bbafeab572c"
